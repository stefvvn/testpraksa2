@model IEnumerable<Facebook.Entities.PostLikeEntities>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PostId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostLikeStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PostId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostLikeStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PostLikeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PostLikeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PostLikeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
